% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparseKNNCross.R
\name{sparseKNNCross}
\alias{sparseKNNCross}
\title{Bidirectional k-nearest-neighbour search from a single pass}
\usage{
sparseKNNCross(
  X,
  Y,
  k = 10L,
  method = c("binary", "jaccard", "cosine", "euclidean", "manhattan", "pearson", "js",
    "covariance"),
  dist = TRUE,
  drop_degenerate = TRUE,
  block_size = 256L,
  ncores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{X, Y}{Sparse column matrices with the same number of rows. Unlike
\code{\link[=sparseKNN]{sparseKNN()}}, \code{Y} is required: with one matrix the two directions coincide
and \code{\link[=sparseKNN]{sparseKNN()}} already exploits the symmetry through \code{full}/\code{diag}.}

\item{k}{Neighbours to return. Truncated INDEPENDENTLY per direction if it
exceeds the candidates available on that side, matching what the
corresponding \code{\link[=sparseKNN]{sparseKNN()}} call would do.}

\item{method, dist, drop_degenerate, block_size, ncores, verbose}{As in
\code{\link[=sparseKNN]{sparseKNN()}}.}
}
\value{
A list of two elements, \code{forward} and \code{reverse}, each a list of \code{idx}
and \code{dist} matrices with the same layout and \code{self_search} attribute as a
\code{\link[=sparseKNN]{sparseKNN()}} result. \code{forward} is indexed by the columns of \code{X} and points
into \code{Y}; \code{reverse} is indexed by the columns of \code{Y} and points into \code{X}.
}
\description{
Computes the neighbours of every column of \code{X} among the columns of \code{Y} AND
the neighbours of every column of \code{Y} among the columns of \code{X}, from one set
of distance blocks.
}
\details{
The slab for a block of query columns already contains every pairwise value
needed for both directions -- \code{sparseKNN(X, Y)} reduces it down its columns
and throws the rest away, and a second call to \code{sparseKNN(Y, X)} recomputes
the identical numbers to reduce them across its rows. This function reduces
in both directions from the same slab, so the distance work is done once.

In a sharded all-pairs search this halves the grid: shard pairs (a, b) and
(b, a) collapse into one task. The reverse direction cannot be finalised
block by block -- a reference column has been compared only against the
queries seen so far -- so it is accumulated in a running state by
\code{topKRowsAccum()}.
}
\section{Equivalence}{

By construction, for any inputs and any \code{block_size}:
\preformatted{
  identical(sparseKNNCross(X, Y, k)$forward, sparseKNN(X, Y, k))
  identical(sparseKNNCross(X, Y, k)$reverse, sparseKNN(Y, X, k))
}
This is the definition the test suite enforces; treat any deviation as a bug
in this function rather than as a tolerable difference.
}

\seealso{
\code{\link[=sparseKNN]{sparseKNN()}}
}
